function solution(n, k) {
    let yang = 12000;
    let drink = 2000;
    
    let sub = Math.floor(n/10);
    console.log(sub)
            return (yang * n) + (drink * (k - sub) )
        
        
}